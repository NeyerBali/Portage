export const environment = {
  production: true,
  // ⚠️ APRÈS avoir déployé l'API sur Render, remplace l'URL ci-dessous par
  // l'URL publique de ton service (en gardant le suffixe /api/), par exemple :
  //   https://porteo-api.onrender.com/api/
  // Puis relance :  ng build --configuration production  &&  firebase deploy
  apiUrl: 'https://REMPLACE-PAR-TON-API.onrender.com/api/'
};
